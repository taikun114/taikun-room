import { redirect } from "react-router";
import { url, status } from "../__generated__/[2021].[07].[can-big-tv-use-as-pc-display.html]._index";

export const loader = () => {
  throw redirect(url, status);
};
