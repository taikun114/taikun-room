import { redirect } from "react-router";
import { url, status } from "../__generated__/[2020].[11].[macos11-how-to-fix-time-machine-backup.html]._index";

export const loader = () => {
  throw redirect(url, status);
};
