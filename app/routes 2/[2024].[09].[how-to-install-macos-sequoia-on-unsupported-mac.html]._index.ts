import { redirect } from "react-router";
import { url, status } from "../__generated__/[2024].[09].[how-to-install-macos-sequoia-on-unsupported-mac.html]._index";

export const loader = () => {
  throw redirect(url, status);
};
