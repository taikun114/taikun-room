import { redirect } from "react-router";
import { url, status } from "../__generated__/[2024].[11].[how-to-update-firmware-using-terminal-command.html]._index";

export const loader = () => {
  throw redirect(url, status);
};
